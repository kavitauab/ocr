# dependencies
/node_modules

# env files
/.env
api/.env
!.env.example
# dist/api/.env is tracked intentionally (production secrets for Plesk)

# misc
.DS_Store
*.pem
npm-debug.log*

# typescript
*.tsbuildinfo

# uploads
/uploads/

# keep dist/ committed for Plesk deployment
# dist/api/.env must be committed (production secrets)
